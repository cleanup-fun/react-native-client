import {
  StyleSheet,
} from 'react-native';

const repeatedStyles = StyleSheet.create({
  centeringContainer: {
    flex: 1,
    alignSelf: 'stretch',
    justifyContent: 'center',
    alignItems: "center"
  }
});


export { repeatedStyles }
